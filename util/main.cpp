#include <iostream>
#include <vector>
#include "iterator.hpp"
int main()
{
    ft::iterator<int> a;
    std::reverse_iterator e;
    std::iterator_traits<int> l;
    std::iterator<int> b;

    if (a >= b)
        std::cout<<" lsfjklsaj\n";
}